<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.clinic">

    <application
        android:allowBackup="true"
        android:appComponentFactory="whateverString"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:overrideLibrary="com.google.firebase.auth"
        tools:replace="android:appComponentFactory">

        <activity android:name=".EmployeeProfile" />
        <activity android:name=".addAvaliability"></activity>
        <activity android:name=".EmployeeAddServices"></activity>
        <activity android:name=".EmployeeMainScreen" />
        <activity android:name=".Calendar" />
        <activity android:name=".AccountsManagement" />
        <activity android:name=".AdminControlPanel" />
        <activity android:name=".WelcomePage" />
        <activity android:name=".createLogin" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Services" />
        <activity android:name=".ServiceInfo" />
    </application>

</manifest>
